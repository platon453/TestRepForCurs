Задание:

Напиши функцию delay(ms), которая будет принимать параметр ms (время задержки в миллисекундах) и возвращать промис, который разрешится через указанное время.

Затем создай асинхронную функцию processData(), которая будет:

Логировать "Загружаем данные...".

Дожидаться завершения задержки с помощью функции delay(ms) (задержка должна быть 3 секунды).

После завершения задержки, логировать "Данные загружены!".

Требования:

Используй delay(ms) для задержки.

Применяй async/await для синхронного выполнения.

Логируй каждый шаг: когда начинается загрузка и когда данные загружены.

Подсказка:

Ты уже знаешь, как использовать Promise и setTimeout, теперь твоя цель — применить await и сделать код асинхронным и читаемым.